[pytest]
# Where pytest looks for tests
testpaths = tests

# Verbose output by default — shows each test name and pass/fail
addopts = -v

# asyncio mode: "strict" = you must decorate async tests with @pytest.mark.asyncio
# Change to "auto" to remove that requirement (trades explicitness for convenience)
asyncio_mode = strict
